// What the map says to do — DERIVED from the graded parts, never authored independently.
// Empty until the whole project has been graded once: themes come from findings, and a fresh map
// has none. The board handles this and simply says there is no roadmap yet.
//
// When it does get written, it holds exactly three things: THEMES (which findings are actually one
// finding, with each part's evidence), ORDER (what unblocks what), and WHAT EACH ONE BUYS (which
// parts move up which floor). If it ever disagrees with a part, the part is right.
window.ROADMAP = null;
